name: CI/CD Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_db
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: "1.25"

      - name: Cache Go modules
        uses: actions/cache@v3
        with:
          path: ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Install dependencies
        run: go mod download

      - name: Run tests
        run: make test
        env:
          DB_HOST: localhost
          DB_PORT: 5432
          DB_USER: postgres
          DB_PASSWORD: postgres
          DB_NAME: test_db

      - name: Run linter
        run: |
          go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest
          golangci-lint run

  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: "1.25"

      - name: Build application
        run: |
          go mod download
          make linux_64
          chmod +x ./out/*

      - name: Upload linux_64 binary as artifact
        uses: actions/upload-artifact@v4
        with:
          name: linux_64
          path: out/main_linux_64

  docker-build:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Download linux_64 binary
        uses: actions/download-artifact@v4
        with:
          name: linux_64
          path: ./out/

      - name: Get necessary files
        run: |
          echo ${{ secrets.FIREBASE_SERVICE_ACCOUNT }} > ./firebase-service-account.json
          echo ${{ secrets.ENV_FILE }} > ./.env
          echo ${{ vars.RBAC_MODEL_CONF }} > ./rbac_model.conf
          echo ${{ vars.RBAC_POLICY_CSV }} > ./rbac_policy.csv

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/cim-backend:latest
            ghcr.io/${{ github.repository_owner }}/cim-backend:${{ github.sha }}
            ghcr.io/${{ github.repository_owner }}/cim-backend:${{ github.ref_name }}
